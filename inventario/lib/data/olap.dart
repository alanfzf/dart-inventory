class Olap{


    final String prod;
    final String cat;
    final DateTime date;
    final String tipo;
    final int cantidad;
    final num total;

  Olap(this.prod, this.cat, this.date, this.tipo, this.cantidad, this.total);

}